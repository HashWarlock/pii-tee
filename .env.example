# ===================================================
# PII-TEE Environment Configuration
# ===================================================

# Redis Configuration (Required)
# --------------------------------
REDIS_PASSWORD="your_secure_redis_password_here"       # Required: Strong password for Redis

# Service Ports
# --------------------------------
API_PORT=8080                                          # API service port (default: 8080)
CLIENT_PORT=3000                                       # Frontend port (default: 3000)
LISTEN_IP="127.0.0.1"                                 # IP to bind services (default: localhost only)
EXTERNAL_HOST="localhost"                              # External hostname for API calls

# Application Settings
# --------------------------------
NODE_ENV="production"                                  # Environment: development | production
NEXT_PUBLIC_APP_URL="http://localhost:3000"           # Public URL for the frontend

# Optional: AI Provider API Keys
# --------------------------------
# Add any API keys for AI providers if using Task Master features
# ANTHROPIC_API_KEY="sk-ant-api03-..."                 # Claude models
# OPENAI_API_KEY="sk-proj-..."                        # GPT models
# PERPLEXITY_API_KEY="pplx-..."                       # Research features

# Optional: External Services
# --------------------------------
# GOOGLE_SITE_VERIFICATION="your_verification_code"    # Google site verification